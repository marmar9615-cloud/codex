name: Mobile iOS E2E

on:
  workflow_dispatch: {}
  pull_request:
    paths:
      - "apps/mobile/**"
      - "packages/mobile-protocol/**"
      - "services/mobile-runner/**"
      - ".maestro/**"

jobs:
  build_ios_e2e:
    name: Build iOS simulator app for E2E
    type: build
    params:
      platform: ios
      profile: e2e-simulator

  maestro_ios:
    name: Run Maestro iOS flows
    needs: [build_ios_e2e]
    type: maestro
    params:
      build_id: ${{ needs.build_ios_e2e.outputs.build_id }}
      flow_path:
        - .maestro/sample_project_flow.yml
        - .maestro/fake_agent_patch_flow.yml
        - .maestro/fake_build_runner_flow.yml
        - .maestro/fake_git_publish_flow.yml
